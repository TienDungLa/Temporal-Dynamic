#spring:
#  application:
#    name: "Temporal Dynacmic"
#  datasource:
#    url: jdbc:postgresql://localhost:5432/postgres
#    username: postgres
#    password: "1234"
#    driver-class-name: org.postgresql.Driver

server:
  port: 8080

temporal:
  host: 127.0.0.1
  port: 7233
  namespace: default

spring:
  datasource:
    url: jdbc:h2:file:./data/testdb
    driverClassName: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
